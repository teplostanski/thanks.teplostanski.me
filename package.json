{
  "name": "thanks.teplostanski.me",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && gh-pages --dotfiles -d dist"
  },
  "dependencies": {
    "@fontsource-variable/sora": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "astro": "^7.1.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
